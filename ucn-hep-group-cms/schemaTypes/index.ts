// Here are the schema types of the document schemas defined in your project. You can import them  and add them to the schemaTypes array to make them available in your Sanity Studio.

import { personType } from "./personTypes";

export const schemaTypes = [personType]